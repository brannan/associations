{
  "name": "associations",
  "version": "1.0.0",
  "description": "associations and migration tutorial",
  "main": "index.js",
  "author": "psb",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.7.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.7.0",
    "uuid": "^8.3.2"
  },
  "scripts": {
    "dev": "nodemon -r dotenv/config app.js",
    "db:create": "sequelize-cli db:create",
    "db:migrate": "sequelize-cli db:migrate",
    "db:migrate:undo:all": "sequelize-cli db:migrate:undo:all",
    "db:g:seed": "sequelize-cli seed:generate --name",
    "db:seeds": "sequelize-cli db:seed:all",
    "db:seeds:undo": "sequelize-cli db:seed:undo"
  },
  "devDependencies": {
    "sequelize-cli": "^6.2.0"
  }
}
